use std::cmp::min;

struct Config {
    name: str,
    addr: str,
    key: str,
}

fn main() {
    let my_s = "Hello, world.";
    println!("first use: {}", min(3, 7));
    println!("len: {}", my_s.len());
}
